module.exports = {
  projects: [
    '<rootDir>/src/jest.config.js',
    '<rootDir>/utils/jest.config.js',
    '<rootDir>/integration-tests/jest.config.js'
  ]
};
// Note 'docs' is not included in the projects array. This is because there are currently no `ts` files in this directory.
